Update 2 is on the way!
This small update changes a lot of UI elements, like this text's container.
Fonts are now installed automatically, so if they're not already applied then you need to restart Jiayi.
Enjoy!